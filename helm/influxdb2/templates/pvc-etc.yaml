---
apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "influxdb2-etc-claim"
spec:
  accessModes:
  - "ReadWriteMany"
  volumeMode: "Filesystem"
  resources:
    requests:
      storage: "1Gi"
